In order to use this, copy the entire allFlows.txt to the clipboard. Then inside Node-RED import via the clipboard. 
Dependencies will be noted by node-red and can be installed directly from the palette. Python scripts needed should be saved locally,
and the node that calls them should be directed to the path to the scripts (inside the node configuration).